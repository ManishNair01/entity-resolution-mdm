.venv/
__pycache__/
*.pyc
.pytest_cache/

# Generated artefacts: the pipeline rebuilds these from raw data.
data/mdm.duckdb
data/mdm.duckdb.wal

# metrics.json is regenerated every run and would churn on every commit.
# The reports themselves (profile.md, model_evaluation.md, charts) are
# deliverables and belong in the repo.
reports/metrics.json
